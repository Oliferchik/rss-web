const HTTP_METHODS = {
  GET: 'GET',
  POST: 'POST',
};

const DB_TABLES = {
  USERS: 'users',
  TELEGRAM_CLIENTS: 'telegram_clients',
  RSS: 'rss',
};

const REGION = 'eu-north-1';

module.exports = {
  HTTP_METHODS,
  DB_TABLES,
  REGION,
};
